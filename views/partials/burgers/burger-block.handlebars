<tr class="table-row">
    <td class="table-first-col">{{id}}. {{burger_name}}</td>
    <td><button class="change-devour" data-id="{{id}}">Eat it!</button>
    <button class="delete-B" data-id="{{id}}">DELETE!</button></td> 
</tr>
